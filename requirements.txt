# Requirements for Let's Talk Forum Data Analysis
# Installation: pip install -r requirements.txt

# Core Data Analysis
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0  # For reading Excel files

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress Bars
tqdm>=4.65.0

# NLP & Text Analysis (Optional - only if using advanced NLP features)
textblob>=0.17.0
scikit-learn>=1.3.0
wordcloud>=1.9.0
nltk>=3.8.0

# Transformers & Deep Learning (Optional - only if using RoBERTa classification)
torch>=2.0.0
transformers>=4.30.0

# Note: The keyword-based classification script only needs the packages above the NLP section
# For basic analysis, you only need: pandas, numpy, openpyxl, matplotlib, seaborn, tqdm